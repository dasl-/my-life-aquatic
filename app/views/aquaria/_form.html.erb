<%= form_for(@aquarium) do |f| %>
  <% if @aquarium.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@aquarium.errors.count, "error") %> prohibited this aquarium from being saved:</h2>

      <ul>
      <% @aquarium.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
